services:
  flatpare:
    build:
      context: .
      dockerfile: Dockerfile
    image: flatpare:local
    ports:
      - "3000:3000"
    environment:
      APP_PASSWORD: ${APP_PASSWORD}
      FIREWORKS_API_KEY: ${FIREWORKS_API_KEY}
      FIREWORKS_MODEL: ${FIREWORKS_MODEL}
      PORT: 3000
